# Copie este arquivo para .env (fora do controle de versão) e preencha os valores reais.
# O .env NUNCA deve ser commitado no repositório.

DB_HOST=localhost
DB_NAME=gpciuc95_deposito
DB_USER=gpciuc95_db
DB_PASS=troque-esta-senha
DB_CHARSET=utf8

# SIAPE do usuário master que recebe notificações administrativas
SCM_SIAPE_MASTER=0000000

# Email remetente para notificações do sistema
SCM_EMAIL_REMETENTE=scm@gpciu.com.br
